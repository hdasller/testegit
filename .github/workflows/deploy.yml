name: deploy
on:
  workflow_dispatch:
    inputs:
      env:
        description: Environment (dev, qa, prod or sandbox)
        default: dev
        required: true

jobs:
  bump-version:
    name: Teste env
    runs-on: ubuntu-latest
    steps:
      - name: Deploy tag
        run: echo ${{ github.event.inputs.env }}
