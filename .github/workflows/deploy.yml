name: Deploy manual

on:
  workflow_dispatch:
    inputs:
      tbranch:
        description: Digite a branch a ser deployada em Homologação
        default: homolog
        required: true

jobs:
  run-deploy:
     runs-on: ubuntu-latest 
     steps:
      - name: Dispatch action and pass 
        run: >-
        curl --location --request GET 'https://api.github.com/repos/hdasller/testegit/actions/workflows/11506890/dispatches' \
        --header 'Authorization: Bearer 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' \
      - name: Repo
        run: echo Repo ${{ github.repository }}
        
      - name: Repo
        run: echo Repo ${{ github.repository }}