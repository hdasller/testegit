name: Deploy manual

on:
  workflow_dispatch:

jobs:
  run-deploy:
     runs-on: ubuntu-latest 
     steps:
      - name: Dispatch action and pass 
        run: | 
          echo  Vercel token ${{ github.VERCEL_TOKEN }}}